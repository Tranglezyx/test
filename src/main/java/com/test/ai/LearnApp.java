package com.test.ai;

//导入jsr381的包

public class LearnApp {

    public static void main(String[] args) {
//        //创建一个图像工厂
//        ImageFactory imageFactory = ImageFactoryService.getImageFactory();
//
//        ImageClassifier<BufferedImage> imageClassifier = ImageClassifierNetwork.builder()
//                .inputSize(64, 64) //设置输入图像的大小
//                .labelsFile("labels.txt") //设置标签文件的路径
//                .modelFile("model.nnet") //设置模型文件的路径
//                .build(); //构建分类器
////创建一个图像数据集
//        DataSet<BufferedImage> imageDataSet = new DirectoryImageDataSet(imageFactory, "images", true);
//
////对数据集中的每个图像进行分类，并打印结果
//        for (BufferedImage image : imageDataSet) {
//            Map<String, Float> results = imageClassifier.classify(image); //进行分类
//            System.out.println(results); //打印结果
//        }
    }
}
